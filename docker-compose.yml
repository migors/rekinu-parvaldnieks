version: "3.8"

services:
  invoice-app:
    build: .
    container_name: invoice-manager
    ports:
      - "8000:8000"
    volumes:
      - invoice-data:/app/data
    restart: unless-stopped
    environment:
      - TZ=Europe/Riga

volumes:
  invoice-data:
